# Repository Guidance

## 项目定位与信息源

- 本仓库负责价格数据的采集、规范化、校验、审计与 MySQL 持久化；V1 设备商城是历史 demo，V2 面向政府生鲜与官方电子设备，同时保留全品类通用表结构。接入状态以开发文档和代码为准。
- `README.md` 和 `docs/README.md` 是入口，`docs/V2_GENERAL_CATALOG_DEVELOPMENT_PLAN.md` 是当前范围与开发基准，`docs/PROJECT_STATUS.md` 集中维护验收与遗留项；`docs/archive/` 仅作历史追溯，代码和测试代表当前实现状态。
- 若需求改变业务范围、价格口径、数据库结构或运行方式，应同步更新相应文档，不要只在代码中形成隐含规则。

## 结构与可读性

- 保持仓库根目录整洁；新文件应进入职责最匹配的现有目录，相关实现、测试和 fixture 保持易于相互定位。只有出现清晰的新边界时才增加目录层级。
- 模块、类和函数应具有明确职责，名称使用项目领域语言并表达意图；避免把不相关能力堆入通用工具文件，也不要仅为形式整齐进行大规模搬迁。
- 代码首先服务于阅读和维护。优先采用直接、清晰的控制流；注释解释业务原因、约束和取舍，而不是复述代码表面行为。
- README、运维文档、CLI 帮助、错误消息和配置示例均面向实际使用者编写，清楚说明前置条件、操作、预期结果与失败后的处理方式。用户可见行为变化时同步更新这些入口。

## 工程原则

- 修改前先阅读相关实现、测试和文档，沿既有调用链确定最小且完整的变更范围；保留用户未要求调整的行为。
- 品牌和站点差异应留在 adapter/crawler 层；领域模型、校验、持久化和调度层保持品牌无关。只有形成稳定共性时才抽取共享能力。
- 数据正确性和可追溯性优先于“尽量写入”：不猜测价格或状态，不用条件价、起售价补齐缺失值，不让异常批次覆盖可信数据。
- 保持原始证据、批次记录、当前价格和价格历史之间可追溯；影响这些关系的改动必须考虑失败、重放、幂等和乱序场景。
- 真实来源访问受合规门禁约束。除非用户明确授权且运行配置满足要求，不开启或绕过 live crawl，不规避验证码、登录、签名或访问控制。
- 数据库变更通过 SQLAlchemy 模型与 Alembic 迁移共同演进，并以 MySQL 5.7 和 8.x 验证；不要用 SQLite 代替兼容性测试。
- 不提交密钥、真实凭据、Cookie、完整未脱敏页面或本地运行产物；新增配置时同步维护 `.env.example`。
- 优先做聚焦、可回滚的改动，复用现有结构和依赖；避免无关重构、复制实现或仅为未来假设增加抽象。

## 验证要求

- 根据改动风险选择最小充分验证，并优先运行针对性测试。
- Python 代码通常至少运行 `make lint` 和 `make test-unit`。
- 涉及模型、迁移、Repository、事务或 MySQL 锁时，运行 `RUN_MYSQL_INTEGRATION=1 make test-integration`；不要连接或清理非专用测试库。
- 涉及 Docker、入口命令或运行时权限时，补充镜像构建和相应 CLI smoke test；真实来源 smoke test 仍须遵守合规门禁。
- 仅修改文档时，检查链接、命令和代码现状是否一致。若某项验证因环境限制无法执行，明确说明原因，不把“未运行”表述为“已通过”。

## 完成标准

- 新行为有与风险相称的测试，失败路径和数据一致性得到覆盖。
- 实现、目录结构、迁移、配置、测试和面向用户的文档保持一致，不遗留无说明的临时文件或调试开关。
- 交付说明应简要列出变更、验证结果、已知限制和需要人工确认的事项。

## 维护本文件

- 保持本文件简短、稳定且无冲突；阶段进度、页面 selector、字段清单和详细操作手册应放在 README、`docs/`、测试或代码附近。
- 当某个子目录确实形成独立约定时，可在该目录增加更聚焦的 `AGENTS.md`，不要持续膨胀根文件。

## 适度设计约束

- 默认采用满足当前需求的最小实现，不为假设中的未来需求提前增加版本系统、兼容层、哈希链、插件机制或通用抽象。
- 新增机制前必须说明它解决的当前问题、真实调用方及验收方式；“以后可能需要”不能作为理由。
- 哈希仅用于外部交付、不可变内容或明确的损坏/篡改风险；普通配置和可重建数据优先使用 schema、唯一键及业务规则校验。
- 版本化仅在确实需要同时兼容多个格式或外部消费者无法同步升级时引入；只有一个格式时禁止增加 `v1/v2`、`schema_version` 或版本注册表。
- 无法证明额外复杂度必要时，选择更简单的方案。

## 代码修复原则

- 先定位根因，再修改代码。
- 优先修改、重构或删除错误实现，禁止叠加补丁绕过问题。
- 新实现替代旧实现时，必须删除旧代码并迁移调用方。
- 禁止无必要的兼容层、重复实现和特殊分支。
- 完成后清理死代码，确保只有一条权威实现路径。
