# 项目状态与遗留事项

> 整理日期：2026-09-12；代码基线：`024b76a`。
>
> 公司数据及验证结果来自 2026-09-11 的阶段 M 记录；本次文档整理没有重新连接公司库或采集网站。

## 1. 阶段性结论

核心功能已实现：两个政府生鲜来源和五品牌官方设备直接采集到 V2，支持规范化、质量校验、原始证据、MySQL 持久化、只读重放和审计。V1 运行代码已退出，物理历史表及数据仍保留，不需要先从 V1 迁入数据。

政府 A～H、设备 I～L 已完成。M 的备份恢复、公司增量迁移、五品牌采集及代码回归已完成，**但最终验收未通过**：严格重放发现 6 组历史 iMac 重复来源身份。代码提交和合并 `dev` 不等同于数据问题已修复。完整证据见[阶段 M 验收记录](archive/v2/V2_PHASE_M_ACCEPTANCE_REPORT.md)。

## 2. 最近一次公司数据快照

| 项目 | 2026-09-11 已核验结果 |
| --- | --- |
| MySQL / Alembic head | `5.7.36-log` / `b72c910e4f31` |
| 表结构 | 13 张 V2 运行表；完整历史迁移共 23 张业务表，另有 Alembic 版本表；40 个 MySQL 5.7 校验触发器 |
| 电子设备 | 104 个标准型号、1,129 个标准规格；1,135 条来源当前记录，其中包含 6 条 iMac 重复身份记录 |
| 政府生鲜 | 1,362 条当前记录：商务部 1,354、上海 8；该轮设备采集未刷新政府报价 |
| V2 总量 | 2,497 条当前记录、2,793 条价格观察、165 条证据记录、25 个批次 |
| 隔离核验 | V1、政府全部既有业务行未变，其他 schema 的结构元数据未变；不代表逐行导出或核验了其他库业务数据 |
| 运行方式 | 默认手工执行；验收结束时无后台采集，未开启持续调度 |

政府来源日期仍分别为北京时间 2026-08-23、2026-08-25，不能称为 9 月的新价格。“当前”只表示库内最新可信记录；是否足够新必须看 `observed_at`，不能用下载时间替代。

设备最近一次复采：华为、小米、OPPO、vivo 批次成功；Apple 为 `PARTIAL`，错误包含 3 个 Watch 解析缺口和 1 个 iMac SKU 覆盖告警。采集数量仅对应发现入口返回的产品，不构成全站覆盖承诺。原价缺失保持空值；`UNKNOWN`、缺货和下架都不能当作有货可购买。

## 3. 验证与限制

- lint、461 项单元测试通过；MySQL 5.7.36 和 8.4 各 128 项集成测试通过；镜像构建及离线 CLI smoke 通过。这是代码基线的历史验证结果，不是本次重新运行全部测试。
- 公司数据库审计全部 critical 为 0，165 份证据引用及文件校验通过；政府旧价产生 1,362 条过期提示。
- 严格重放仍因 iMac 身份不一致失败，不能仅凭 `healthy=true` 宣布验收通过。失败验收快照保留，没有生成最终验收成功凭证。

## 4. 后续事项

| 优先级 | 事项 | 完成条件 / 边界 |
| --- | --- | --- |
| 先处理 | 6 组 iMac 重复身份 | 仅合并已核验的身份映射，保留全部价格记录、观察时间和原始证据；先获得限定授权，在隔离副本演练，再处理公司库并复验 |
| 明确保留 | 3 个 Apple Watch 组合总价缺口 | 没有可信的完整配置总价时继续拒绝；不能用表壳、表带单价、起售价或推算价补齐 |
| 按需执行 | 政府报价重采 | 按运行手册先 smoke，再手工采集；同时检查来源日期，来源停更不伪造新时点 |
| 后置 | 更多品种/来源与持续运行 | 以真实样本和访问授权逐个评估；不为 demo 新增监控平台、Redis 或全品类爬虫框架 |

iMac 的 6 组精确 ID 映射、备份位置和修复提议只保留在[原验收记录](archive/v2/V2_PHASE_M_ACCEPTANCE_REPORT.md)，避免在多处维护易过期的数据操作目标。当前没有授权执行这些数据合并，也没有因为整理文档而清理数据库。
