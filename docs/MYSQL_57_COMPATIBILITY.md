# MySQL 5.7.36 兼容说明

> 建立日期：2026-08-11
> 目标实例：公司内网 MySQL 5.7.36
> 目标库：`device_price`
> 当前账号：暂用 root；不得把密码写入 Git、镜像或日志
> 当前状态：阶段 I～L 本地开发完成；代码 head 为 `b72c910e4f31`，公司最近确认版本为 `96524222b3ec`；阶段 M 真实/公司验收待实施，本轮未连接公司库

## 1. 兼容范围

项目最低支持 MySQL 5.7.8，以确保原生 JSON 可用；公司实例为 5.7.36。MySQL 8.x 继续受支持。

连接建立时程序会：

1. 查询并校验服务端版本；
2. 拒绝低于 5.7.8 或无法识别的版本；
3. 执行 `SET SESSION time_zone = '+00:00'`。

数据库约束由 Alembic 迁移安装：MySQL 5.7～8.0.15 使用触发器模式，MySQL 8.0.16+ 使用原生 `CHECK`。普通连接不会安装触发器、修改数据库版本或改变全局配置。

## 2. 数据约束

MySQL 5.7 解析但不执行 `CHECK`。历史迁移 `6f2a91d4c8b7` 会在旧版本上为以下 V1 表建立 `BEFORE INSERT/UPDATE` 触发器；保留这段历史链不代表当前应用继续读写 V1：

- `product`；
- `sales_channel`；
- `crawl_run`；
- `sku`；
- `official_offer`；
- `price_current`；
- `price_history`。

这些历史触发器覆盖状态枚举、CNY/CN/官方直营口径、非负计数、正金额、原价语义和价格历史时间区间。V1 的应用 DTO、校验器和 Repository 已在阶段 L 删除；当前采集只调用 V2 Pydantic、质量门禁和 Repository 事务。

V2 影子迁移 `96524222b3ec` 为 13 张 `v2_` 表补充同类约束，包括 `RETAIL_AVERAGE/WHOLESALE_AVERAGE`、来源日价格、批量证据归属和同日修订身份。完整 V1+V2 head 在 MySQL 5.7 上共有 40 个校验触发器；MySQL 8.0.16+ 使用原生 `CHECK`，不创建这些触发器。

设备阶段 I 追加迁移 `b72c910e4f31`：MySQL 5.7 仅重建 `v2_crawl_record` 和 `v2_price_observation` 的 4 个校验触发器，保留原校验并增加 `PRODUCT/AVAILABILITY_ONLY`；MySQL 8.0.16+ 替换对应的 3 个 CHECK。仍为 23 张业务表、40 个 MySQL 5.7 触发器，不升级数据库版本或改变全局配置。已有新类型事实时，降级在任何 DDL 前拒绝。2026-09-11 已在本地 5.7.36/8.4 完成验证，尚未部署公司库，详见[阶段 I 报告](V2_PHASE_I_BUILD_REPORT.md)。

### 2.1 阶段 L 的运行时与迁移边界

- 唯一 V2 运行时及 SQLAlchemy 元数据只包含 13 张 `v2_` 表；五品牌设备与两个政府来源共用采集、校验和持久化链路。
- 从 Alembic base 升级仍由未改写的历史迁移创建 10 张 V1 和 13 张 V2 业务表；保留旧表及数据，不新增删表迁移。阶段 J～L 没有追加迁移，head 仍为 `b72c910e4f31`。
- `migrations/schema_scope.py` 将自动生成迁移的反射范围限定为 `v2_`，防止已退出 ORM 的 V1 表或同库其他非 V2 表被误判为应删除对象。
- `db check` 只要求 13 张 V2 表；`db audit` 只读检查 V2 数据，不依赖或读取 V1 表，`--check-artifacts` 可额外校验本地证据文件。
- `catalog replay --record-id <ID>` 只读原始证据，在原时点重放全部 SKU 或政府文档行，不联网、不新建批次、不写价格。旧政府上下文可在证据完整时从已保存来源 URL、品种 ID 和发布日期确定恢复。

本地 V2-only 验证和完整历史链验证是不同场景：前者证明运行无需 V1 表，后者证明增量部署不影响保留的历史表。不要为了满足“13 张运行表”而在公司库手动删掉另外 10 张历史表。

## 3. 字符集与时间

- 字符集固定为 `utf8mb4`；
- 兼容排序规则使用 `utf8mb4_general_ci`；
- 业务和审计时间统一按 UTC 写入 `DATETIME(3)`；
- 不依赖服务器 `SYSTEM` 时区；每个物理连接都显式设置 UTC。

## 4. 测试

```bash
make mysql57-up
make test-integration-mysql57
make mysql57-down
```

MySQL 5.7 集成测试必须使用专用 `device_price_test`，禁止把 `TEST_MYSQL_*` 指向公司 `device_price` 或其他已有业务库。测试会执行建表、删表、迁移降级和数据写入。

默认 MySQL 8.4 测试继续保留，用于验证双版本兼容性。

阶段 L 的验证覆盖单一 V2 运行时、只读重放/审计、历史表共存和迁移自动生成范围；具体执行结果以阶段构建报告为准。本地验证不会替代阶段 M 的官方来源及公司环境验收。

## 5. 公司环境上线顺序

以下为空库首次部署顺序。已有 `device_price` 的增量部署不得清库，应先备份并确认当前迁移版本，暂停采集写入后再升级；不能照此重复执行首次建库或测试清理。

1. 确认 `device_price` 是空库；
2. 在隔离 MySQL 5.7.36 完成完整测试；
3. 通过内存注入凭据运行 `alembic upgrade head`；
4. 核对表、`alembic_version` 和触发器：完整历史链 head 为 23 张业务表/40 个 MySQL 5.7 校验触发器，其中当前业务仅使用 13 张 V2 表；
5. 按来源运行 `db seed-v2-government` 或 `db seed-devices`，默认禁用，不启动采集；
6. 执行 `device-price db check` 和只读 `device-price db audit`，确认 V2 结构、UTC、兼容模式与一致性；
7. 真实采集仍须显式启用来源并满足访问门禁，再通过 `catalog smoke/crawl` 验证；不得恢复已删除的 V1 写库入口。

公司库最近确认已存在政府数据且迁移版本为 `96524222b3ec`，因此下一次实际部署应走备份后的增量升级，而不是上述空库首次部署流程。阶段 M 执行前还须重新只读检查当前状态；本轮没有更新这份公司状态快照。

暂用 root 只是一项部署决策，不改变“不落盘、不提交、不记录密码”的密钥规则。后续具备条件时仍应切换到仅授权 `device_price.*` 的账号。

## 6. 公司环境验收记录

以下均为对应日期的历史记录，保留当时版本、表数量和测试结果；不代表本轮已重新连接或验收公司环境。

2026-08-11 已按上述顺序完成首次部署验收：

- 服务端版本：MySQL `5.7.36-log`；
- 目标库：`device_price`，`utf8mb4_general_ci`；
- Alembic 版本：`6f2a91d4c8b7`；
- 业务表：10 张，另有 Alembic 版本表；
- MySQL 5.7 校验触发器：14 个；
- 应用连接会话时区：`+00:00`；
- 基础数据：5 个品牌、6 个分类、5 个官方直营渠道；
- 产品、SKU、offer、当前价格、价格历史、采集批次和采集记录：均为 0；
- 迁移前后对 50 个其他非系统 schema 逐库比较表数量，结果一致。

公司实例未运行会降级迁移、删除表或写入构造业务数据的集成测试；阶段 5 完成后的 79 项单元测试全部通过，13 项数据库集成测试分别在本机专用 MySQL 8.4 和 MySQL 5.7.36 容器全部通过。

### 6.1 V2 重建与采集验收（历史）

2026-08-25 在用户明确授权旧 demo 数据可删除后完成第二次验收：

- 重建前确认实例仍为 `5.7.36-log`、目标库为 `device_price`、没有其他活动会话占用目标库；
- 对目标库做包含表结构、数据和 14 个触发器的完整逻辑备份，记录大小与 SHA-256；
- 只删除并重建 `device_price`，从 Alembic base 升级至 `96524222b3ec`；
- 当前为 23 张业务表和 40 个 MySQL 5.7 校验触发器，会话时区为 `+00:00`；
- 10 张 V1 兼容表均为空，13 张 V2 表保存新采集的政府价格；
- 商务部 15 个品种写入 1,354 条当前批发价，上海 8 个品种写入 8 条当前零售均价；
- 黄瓜单品种重复采集后当前价与观察事实总数均未增加，幂等校验通过；
- 17 个数据库关联原始证据文件均存在且 SHA-256 与记录一致；
- 目标库重建前后，另外 53 个 schema 的 7,308 个表/视图对象及元数据摘要完全一致；
- 175 项单元测试通过，34 项数据库集成测试分别在专用 MySQL 8.4 和 MySQL 5.7.36 通过。

本次验收没有在公司实例运行集成测试，也没有修改数据库版本、全局配置或其他 schema。详细恢复点、数据分布与质量结果见 [V2 阶段 H 公司库验收报告](V2_PHASE_H_COMPANY_ACCEPTANCE_REPORT.md)。

后续于 2026-09-10 单独重采过 V1 设备 demo，因此上文“V1 表为空”仅描述 2026-08-25 验收时点。阶段 I～L 保留这些历史数据，不进行 V1→V2 数据迁移；设备 V2 在阶段 M 通过官方来源直接重采。
